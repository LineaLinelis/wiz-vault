# wiz-vault# Wiz External Memory Vault

- 目的: 会話の重要点を `vault/vault.jsonl` に **1行=1メモ**で追記する
- 追記ルール:
  - 既存行は編集しない（基本は追記のみ）
  - 訂正は新しい行で `links.reinterpretation_of: "<元id>"` を入れる
- 重要タグ: `RELATION:Rinea` は最優先で付ける
